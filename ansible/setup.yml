- name: Klargjør alle servere
  hosts: all
  become: true
  tasks:

    - name: Oppdaterer
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Installer htop
      apt:
        name: htop
        state: present
